#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

touch ~/.theia_bash_history
docker run --rm -it \
  -v ~/.ssh:/root/.ssh \
  -v ~/.aws:/root/.aws \
  -v ~/.theia_bash_history:/root/.bash_history \
  -v "$DIR"/..:/var/www/current \
  -w /var/www/current \
  node:carbon bash
