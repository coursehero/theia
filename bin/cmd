#!/bin/bash

touch ~/.theia_bash_history
docker run --rm -p 3000:3000 -v ~/.ssh:/root/.ssh -v ~/.aws:/root/.aws -v ~/.theia_bash_history:/root/.bash_history -v $(pwd):/var/www/current -w /var/www/current -it node:carbon bash
